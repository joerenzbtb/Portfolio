print("Welcome to the Tip Calculator.")
total_bill = float(input("What was the total bill? $"))
tip = int(input("What percentage tip would you like to give? "))
calc_tip = tip / 100 + 1
tip_total_bill = total_bill * calc_tip
people_splitting = int(input("How many people are splitting the bill? "))
bill_per_person = tip_total_bill / people_splitting
final_total = "{:.2f}".format(bill_per_person)
print(f"Each person should pay ${final_total}.")
